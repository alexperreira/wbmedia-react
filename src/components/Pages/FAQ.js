import React from 'react';
// import { Outlet, Link } from 'react-router-dom';
import Header from '../Sections/Header';

const FAQ = () => {
	return (
		<div>
			<Header />
		</div>
	);
};

export default FAQ;
